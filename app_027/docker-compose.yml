version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8027:8000"
    environment:
      - DEBUG=True
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3027:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8027
    volumes:
      - ./frontend:/app
      - /app/node_modules

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app027
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5459:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6406:6379"

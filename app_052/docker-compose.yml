version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8052:8000"
    environment:
      - DEBUG=True
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3052:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8052
    volumes:
      - ./frontend:/app
      - /app/node_modules

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app052
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5484:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6431:6379"

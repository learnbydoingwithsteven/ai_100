version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8031:8000"
    environment:
      - DEBUG=True
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3031:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8031
    volumes:
      - ./frontend:/app
      - /app/node_modules

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app031
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5463:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6410:6379"

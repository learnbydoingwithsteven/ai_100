version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8029:8000"
    environment:
      - DEBUG=True
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3029:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8029
    volumes:
      - ./frontend:/app
      - /app/node_modules

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app029
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5461:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6408:6379"

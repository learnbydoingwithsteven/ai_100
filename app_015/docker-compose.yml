version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8015:8000"
    environment:
      - DEBUG=True
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3015:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8015
    volumes:
      - ./frontend:/app
      - /app/node_modules

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app015
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5447:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6394:6379"

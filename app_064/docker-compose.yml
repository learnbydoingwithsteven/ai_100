version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8064:8000"
    environment:
      - DEBUG=True
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3064:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8064
    volumes:
      - ./frontend:/app
      - /app/node_modules

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app064
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5496:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6443:6379"

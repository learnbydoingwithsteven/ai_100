version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8066:8000"
    environment:
      - DEBUG=True
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3066:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8066
    volumes:
      - ./frontend:/app
      - /app/node_modules

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app066
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5498:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6445:6379"
